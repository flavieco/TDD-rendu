info:
  name: Cleanup Delete
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{TAIGA_URL}}/issues/{{delete_id}}"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.getStatus() < 400) {
          bru.setNextRequest("Cleanup List");
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
