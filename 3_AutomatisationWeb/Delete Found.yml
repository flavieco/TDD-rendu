info:
  name: Delete Found
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{TAIGA_URL}}/issues/{{found_issue_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
